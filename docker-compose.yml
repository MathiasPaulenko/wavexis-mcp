services:
  wavexis-mcp:
    build: .
    restart: unless-stopped
    command: ["--transport=http", "--host=0.0.0.0", "--port=8765", "--caps=all"]
    ports:
      - "127.0.0.1:8765:8765"
    environment:
      - WAVEXIS_BROWSER_PATH=/usr/bin/chromium
      - WAVEXIS_MCP_OUTPUT_DIR=/home/wavexis/output
      - CI=true
    volumes:
      - ./output:/home/wavexis/output
